diff --git a/cuda_rasterizer/backward.cu b/cuda_rasterizer/backward.cu
index 006b1ef..df42ed8 100644
--- a/cuda_rasterizer/backward.cu
+++ b/cuda_rasterizer/backward.cu
@@ -407,7 +407,7 @@ __global__ void preprocessCUDA(
 	const float scale_modifier,
 	const float* proj,
 	const glm::vec3* campos,
-	const float3* dL_dmean2D,
+	const float4* dL_dmean2D,
 	glm::vec3* dL_dmeans,
 	float* dL_dcolor,
 	float* dL_dcov3D,
@@ -464,7 +464,7 @@ renderCUDA(
 	const uint32_t* __restrict__ n_contrib,
 	const float* __restrict__ dL_dpixels,
 	const float* __restrict__ dL_invdepths,
-	float3* __restrict__ dL_dmean2D,
+	float4* __restrict__ dL_dmean2D,
 	float4* __restrict__ dL_dconic2D,
 	float* __restrict__ dL_dopacity,
 	float* __restrict__ dL_dcolors,
@@ -622,9 +622,16 @@ renderCUDA(
 			const float dG_ddelx = -gdx * con_o.x - gdy * con_o.y;
 			const float dG_ddely = -gdy * con_o.z - gdx * con_o.y;
 
-			// Update gradients w.r.t. 2D mean position of the Gaussian
-			atomicAdd(&dL_dmean2D[global_id].x, dL_dG * dG_ddelx * ddelx_dx);
-			atomicAdd(&dL_dmean2D[global_id].y, dL_dG * dG_ddely * ddely_dy);
+			// Update gradients w.r.t. the 2D mean position.  The first two
+			// channels preserve the original signed sum; the last two sum
+			// absolute per-pixel contributions and therefore avoid gradient
+			// cancellation (AbsGS/Improved-GS).
+			const float grad_x = dL_dG * dG_ddelx * ddelx_dx;
+			const float grad_y = dL_dG * dG_ddely * ddely_dy;
+			atomicAdd(&dL_dmean2D[global_id].x, grad_x);
+			atomicAdd(&dL_dmean2D[global_id].y, grad_y);
+			atomicAdd(&dL_dmean2D[global_id].z, fabsf(grad_x));
+			atomicAdd(&dL_dmean2D[global_id].w, fabsf(grad_y));
 
 			// Update gradients w.r.t. 2D covariance (2x2 matrix, symmetric)
 			atomicAdd(&dL_dconic2D[global_id].x, -0.5f * gdx * d.x * dL_dG);
@@ -653,7 +660,7 @@ void BACKWARD::preprocess(
 	const float focal_x, float focal_y,
 	const float tan_fovx, float tan_fovy,
 	const glm::vec3* campos,
-	const float3* dL_dmean2D,
+	const float4* dL_dmean2D,
 	const float* dL_dconic,
 	const float* dL_dinvdepth,
 	float* dL_dopacity,
@@ -701,7 +708,7 @@ void BACKWARD::preprocess(
 		scale_modifier,
 		projmatrix,
 		campos,
-		(float3*)dL_dmean2D,
+		(float4*)dL_dmean2D,
 		(glm::vec3*)dL_dmean3D,
 		dL_dcolor,
 		dL_dcov3D,
@@ -725,7 +732,7 @@ void BACKWARD::render(
 	const uint32_t* n_contrib,
 	const float* dL_dpixels,
 	const float* dL_invdepths,
-	float3* dL_dmean2D,
+	float4* dL_dmean2D,
 	float4* dL_dconic2D,
 	float* dL_dopacity,
 	float* dL_dcolors,
diff --git a/cuda_rasterizer/backward.h b/cuda_rasterizer/backward.h
index 4d02560..80f5238 100644
--- a/cuda_rasterizer/backward.h
+++ b/cuda_rasterizer/backward.h
@@ -34,7 +34,7 @@ namespace BACKWARD
 		const uint32_t* n_contrib,
 		const float* dL_dpixels,
 		const float* dL_invdepths,
-		float3* dL_dmean2D,
+		float4* dL_dmean2D,
 		float4* dL_dconic2D,
 		float* dL_dopacity,
 		float* dL_dcolors,
@@ -56,7 +56,7 @@ namespace BACKWARD
 		const float focal_x, float focal_y,
 		const float tan_fovx, float tan_fovy,
 		const glm::vec3* campos,
-		const float3* dL_dmean2D,
+		const float4* dL_dmean2D,
 		const float* dL_dconics,
 		const float* dL_dinvdepth,
 		float* dL_dopacity,
diff --git a/cuda_rasterizer/forward.cu b/cuda_rasterizer/forward.cu
index c5e01dd..67b97bb 100644
--- a/cuda_rasterizer/forward.cu
+++ b/cuda_rasterizer/forward.cu
@@ -285,7 +285,9 @@ renderCUDA(
 	const float* __restrict__ bg_color,
 	float* __restrict__ out_color,
 	const float* __restrict__ depths,
-	float* __restrict__ invdepth)
+	float* __restrict__ invdepth,
+	const float* __restrict__ pixel_weights,
+	float* __restrict__ accum_weights)
 {
 	// Identify current tile and associated min/max pixel range.
 	auto block = cg::this_thread_block();
@@ -374,6 +376,12 @@ renderCUDA(
 			if(invdepth)
 			expected_invdepth += (1 / depths[collected_id[j]]) * alpha * T;
 
+			// Edge-Aware Score: accumulate the weighted rendering
+			// contribution of this Gaussian over all pixels.  This follows
+			// the same front-to-back compositing weight used for RGB/depth.
+			if (pixel_weights != nullptr && accum_weights != nullptr)
+				atomicAdd(&accum_weights[collected_id[j]], pixel_weights[pix_id] * alpha * T);
+
 			T = test_T;
 
 			// Keep track of last range entry to update this
@@ -409,7 +417,9 @@ void FORWARD::render(
 	const float* bg_color,
 	float* out_color,
 	float* depths,
-	float* depth)
+	float* depth,
+	const float* pixel_weights,
+	float* accum_weights)
 {
 	renderCUDA<NUM_CHANNELS> << <grid, block >> > (
 		ranges,
@@ -423,7 +433,9 @@ void FORWARD::render(
 		bg_color,
 		out_color,
 		depths, 
-		depth);
+		depth,
+		pixel_weights,
+		accum_weights);
 }
 
 void FORWARD::preprocess(int P, int D, int M,
diff --git a/cuda_rasterizer/forward.h b/cuda_rasterizer/forward.h
index 5b18005..a0b75cd 100644
--- a/cuda_rasterizer/forward.h
+++ b/cuda_rasterizer/forward.h
@@ -62,7 +62,9 @@ namespace FORWARD
 		const float* bg_color,
 		float* out_color,
 		float* depths,
-		float* depth);
+		float* depth,
+		const float* pixel_weights,
+		float* accum_weights);
 }
 
 
diff --git a/cuda_rasterizer/rasterizer.h b/cuda_rasterizer/rasterizer.h
index 64afdee..133b7ca 100644
--- a/cuda_rasterizer/rasterizer.h
+++ b/cuda_rasterizer/rasterizer.h
@@ -50,6 +50,8 @@ namespace CudaRasterizer
 			const bool prefiltered,
 			float* out_color,
 			float* depth,
+			const float* pixel_weights,
+			float* accum_weights,
 			bool antialiasing,
 			int* radii = nullptr,
 			bool debug = false);
diff --git a/cuda_rasterizer/rasterizer_impl.cu b/cuda_rasterizer/rasterizer_impl.cu
index 41c4ed3..80f0bd8 100644
--- a/cuda_rasterizer/rasterizer_impl.cu
+++ b/cuda_rasterizer/rasterizer_impl.cu
@@ -217,6 +217,8 @@ int CudaRasterizer::Rasterizer::forward(
 	const bool prefiltered,
 	float* out_color,
 	float* depth,
+	const float* pixel_weights,
+	float* accum_weights,
 	bool antialiasing,
 	int* radii,
 	bool debug)
@@ -335,7 +337,9 @@ int CudaRasterizer::Rasterizer::forward(
 		background,
 		out_color,
 		geomState.depths,
-		depth), debug)
+		depth,
+		pixel_weights,
+		accum_weights), debug)
 
 	return num_rendered;
 }
@@ -411,7 +415,7 @@ void CudaRasterizer::Rasterizer::backward(
 		imgState.n_contrib,
 		dL_dpix,
 		dL_invdepths,
-		(float3*)dL_dmean2D,
+		(float4*)dL_dmean2D,
 		(float4*)dL_dconic,
 		dL_dopacity,
 		dL_dcolor,
@@ -436,7 +440,7 @@ void CudaRasterizer::Rasterizer::backward(
 		focal_x, focal_y,
 		tan_fovx, tan_fovy,
 		(glm::vec3*)campos,
-		(float3*)dL_dmean2D,
+		(float4*)dL_dmean2D,
 		dL_dconic,
 		dL_dinvdepth,
 		dL_dopacity,
diff --git a/diff_gaussian_rasterization/__init__.py b/diff_gaussian_rasterization/__init__.py
index 7f228ce..0205818 100644
--- a/diff_gaussian_rasterization/__init__.py
+++ b/diff_gaussian_rasterization/__init__.py
@@ -29,7 +29,7 @@ def rasterize_gaussians(
     cov3Ds_precomp,
     raster_settings,
 ):
-    return _RasterizeGaussians.apply(
+    result = _RasterizeGaussians.apply(
         means3D,
         means2D,
         sh,
@@ -40,6 +40,12 @@ def rasterize_gaussians(
         cov3Ds_precomp,
         raster_settings,
     )
+    # Keep the public API compatible with the original rasterizer.  The
+    # per-Gaussian accumulation is only exposed when pixel weights are
+    # explicitly requested (Improved-GS EAS).
+    if raster_settings.pixel_weights is None:
+        return result[:3]
+    return result
 
 class _RasterizeGaussians(torch.autograd.Function):
     @staticmethod
@@ -56,6 +62,21 @@ class _RasterizeGaussians(torch.autograd.Function):
         raster_settings,
     ):
 
+        if means2D.ndim != 2 or means2D.shape[1] not in (3, 4):
+            raise ValueError("means2D must have shape (num_points, 3 or 4)")
+
+        pixel_weights = raster_settings.pixel_weights
+        if pixel_weights is None:
+            pixel_weights = torch.empty((0,), dtype=torch.float32, device=means3D.device)
+        else:
+            if pixel_weights.numel() != raster_settings.image_height * raster_settings.image_width:
+                raise ValueError(
+                    "pixel_weights must contain exactly image_height * image_width values"
+                )
+            pixel_weights = pixel_weights.to(
+                device=means3D.device, dtype=torch.float32
+            ).contiguous()
+
         # Restructure arguments the way that the C++ lib expects them
         args = (
             raster_settings.bg, 
@@ -77,20 +98,23 @@ class _RasterizeGaussians(torch.autograd.Function):
             raster_settings.campos,
             raster_settings.prefiltered,
             raster_settings.antialiasing,
+            pixel_weights,
             raster_settings.debug
         )
 
         # Invoke C++/CUDA rasterizer
-        num_rendered, color, radii, geomBuffer, binningBuffer, imgBuffer, invdepths = _C.rasterize_gaussians(*args)
+        num_rendered, color, radii, geomBuffer, binningBuffer, imgBuffer, invdepths, accum_weights = _C.rasterize_gaussians(*args)
 
         # Keep relevant tensors for backward
         ctx.raster_settings = raster_settings
         ctx.num_rendered = num_rendered
+        ctx.means2D_columns = means2D.shape[1]
         ctx.save_for_backward(colors_precomp, means3D, scales, rotations, cov3Ds_precomp, radii, sh, opacities, geomBuffer, binningBuffer, imgBuffer)
-        return color, radii, invdepths
+        ctx.mark_non_differentiable(radii, accum_weights)
+        return color, radii, invdepths, accum_weights
 
     @staticmethod
-    def backward(ctx, grad_out_color, _, grad_out_depth):
+    def backward(ctx, grad_out_color, _, grad_out_depth, _grad_accum_weights):
 
         # Restore necessary values from context
         num_rendered = ctx.num_rendered
@@ -126,6 +150,17 @@ class _RasterizeGaussians(torch.autograd.Function):
         # Compute gradients for relevant tensors by invoking backward method
         grad_means2D, grad_colors_precomp, grad_opacities, grad_means3D, grad_cov3Ds_precomp, grad_sh, grad_scales, grad_rotations = _C.rasterize_gaussians_backward(*args)        
 
+        # CUDA always computes [signed_x, signed_y, abs_x, abs_y].  The
+        # original public API accepted an (N, 3) dummy tensor whose third
+        # component had no geometric meaning and therefore received zero
+        # gradient. Preserve that contract for direct legacy callers instead
+        # of accidentally exposing abs_x as their third component.
+        if ctx.means2D_columns == 3:
+            grad_means2D = torch.cat(
+                (grad_means2D[:, :2], torch.zeros_like(grad_means2D[:, :1])),
+                dim=1,
+            )
+
         grads = (
             grad_means3D,
             grad_means2D,
@@ -154,6 +189,7 @@ class GaussianRasterizationSettings(NamedTuple):
     prefiltered : bool
     debug : bool
     antialiasing : bool
+    pixel_weights : torch.Tensor = None
 
 class GaussianRasterizer(nn.Module):
     def __init__(self, raster_settings):
diff --git a/rasterize_points.cu b/rasterize_points.cu
index e625c19..a47212f 100644
--- a/rasterize_points.cu
+++ b/rasterize_points.cu
@@ -32,7 +32,7 @@ std::function<char*(size_t N)> resizeFunctional(torch::Tensor& t) {
     return lambda;
 }
 
-std::tuple<int, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>
+std::tuple<int, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>
 RasterizeGaussiansCUDA(
 	const torch::Tensor& background,
 	const torch::Tensor& means3D,
@@ -53,6 +53,7 @@ RasterizeGaussiansCUDA(
 	const torch::Tensor& campos,
 	const bool prefiltered,
 	const bool antialiasing,
+	const torch::Tensor& pixel_weights,
 	const bool debug)
 {
   if (means3D.ndimension() != 2 || means3D.size(1) != 3) {
@@ -74,6 +75,21 @@ RasterizeGaussiansCUDA(
   out_invdepthptr = out_invdepth.data<float>();
 
   torch::Tensor radii = torch::full({P}, 0, means3D.options().dtype(torch::kInt32));
+
+  const float* pixel_weight_ptr = nullptr;
+  float* accum_weight_ptr = nullptr;
+  torch::Tensor accum_weights = torch::empty({0}, float_opts);
+  if (pixel_weights.numel() != 0)
+  {
+	TORCH_CHECK(pixel_weights.scalar_type() == torch::kFloat32, "pixel_weights must be float32");
+	TORCH_CHECK(pixel_weights.numel() == H * W, "pixel_weights must contain H * W values");
+	TORCH_CHECK(pixel_weights.is_cuda(), "pixel_weights must be a CUDA tensor");
+	TORCH_CHECK(pixel_weights.device() == means3D.device(), "pixel_weights must be on the means3D device");
+	TORCH_CHECK(pixel_weights.is_contiguous(), "pixel_weights must be contiguous");
+	accum_weights = torch::zeros({P}, float_opts).contiguous();
+	pixel_weight_ptr = pixel_weights.data_ptr<float>();
+	accum_weight_ptr = accum_weights.data_ptr<float>();
+  }
   
   torch::Device device(torch::kCUDA);
   torch::TensorOptions options(torch::kByte);
@@ -116,11 +132,13 @@ RasterizeGaussiansCUDA(
 		prefiltered,
 		out_color.contiguous().data<float>(),
 		out_invdepthptr,
+		pixel_weight_ptr,
+		accum_weight_ptr,
 		antialiasing,
 		radii.contiguous().data<int>(),
 		debug);
   }
-  return std::make_tuple(rendered, out_color, radii, geomBuffer, binningBuffer, imgBuffer, out_invdepth);
+  return std::make_tuple(rendered, out_color, radii, geomBuffer, binningBuffer, imgBuffer, out_invdepth, accum_weights);
 }
 
 std::tuple<torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>
@@ -161,7 +179,9 @@ std::tuple<torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Te
   }
 
   torch::Tensor dL_dmeans3D = torch::zeros({P, 3}, means3D.options());
-  torch::Tensor dL_dmeans2D = torch::zeros({P, 3}, means3D.options());
+  // x/y are the standard signed gradient. z/w accumulate the absolute
+  // per-pixel x/y terms used by AbsGS and Improved-GS.
+  torch::Tensor dL_dmeans2D = torch::zeros({P, 4}, means3D.options());
   torch::Tensor dL_dcolors = torch::zeros({P, NUM_CHANNELS}, means3D.options());
   torch::Tensor dL_dconic = torch::zeros({P, 2, 2}, means3D.options());
   torch::Tensor dL_dopacity = torch::zeros({P, 1}, means3D.options());
diff --git a/rasterize_points.h b/rasterize_points.h
index 82cbd4f..bde7fe4 100644
--- a/rasterize_points.h
+++ b/rasterize_points.h
@@ -15,7 +15,7 @@
 #include <tuple>
 #include <string>
 	
-std::tuple<int, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>
+std::tuple<int, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>
 RasterizeGaussiansCUDA(
 	const torch::Tensor& background,
 	const torch::Tensor& means3D,
@@ -36,6 +36,7 @@ RasterizeGaussiansCUDA(
 	const torch::Tensor& campos,
 	const bool prefiltered,
 	const bool antialiasing,
+	const torch::Tensor& pixel_weights,
 	const bool debug);
 
 std::tuple<torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>

